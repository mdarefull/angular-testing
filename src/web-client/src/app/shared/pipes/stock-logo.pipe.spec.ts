import { StockLogoPipe } from './stock-logo.pipe';

describe('StockLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new StockLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
